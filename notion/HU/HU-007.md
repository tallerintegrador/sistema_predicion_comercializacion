# HU-007 — Endpoint de riesgo de almacén

> **Como** sistema cliente, **quiero** un endpoint HTTP que, dado mi histórico y mi inventario,
> me diga el riesgo de quiebre, el stock recomendado y el segmento de cada tienda, **para**
> integrarlo sin conocer los modelos internos.

- **Capa:** API — Fase 3. **Endpoint:** `POST /inventory`.
- **Código:** `src/spc/api/routers/almacen.py`, `src/spc/api/schemas/almacen.py`,
  `src/spc/service/almacen_service.py`.
- **Motor:** `clasificacion_v1` (HU-003) + `clustering_tiendas_v1` (HU-004). **No usa la regresión.**
- **Tests:** `tests/api/test_almacen_api.py`.

---

## 1. Objetivo y alcance

Exponer el campo **ALMACÉN** del contrato. El cliente envía `history` (para clasificar demanda alta)
y el `inventory_status` por producto (`current_stock`, `lead_time_days?`). La respuesta combina, por
producto: **clase de demanda** (high/low + probabilidad), **riesgo de quiebre** (`stockout_risk`),
**stock recomendado/seguridad** y el **`store_segment`** del clustering. El contrato define ALMACÉN
como **clasificación + perfilado** (por eso **no** usa la regresión).

## 2. Qué se hizo

El servicio (`almacen_service.alertas`, `almacen_service.py:109`) ensambla **tres piezas sin conocer
sus algoritmos**:

### 2.1. Clasificación (riesgo)

- `_clases_por_serie` toma la **última observación** de cada serie `(store_nbr, family)` como régimen
  de demanda actual y le aplica el clasificador, que usa **su propio umbral** (≈0.3185, dentro del
  artefacto — **no se hard-codea**). Devuelve `demand_class` (high/low) y `high_demand_probability`.
- La alineación predicción↔histórico es posicional y se **verifica con un invariante** (`RuntimeError`
  si no alinean).

### 2.2. Clustering de tiendas (segmento)

- `_segmentos_por_tienda` llama a `perfilar` del artefacto de tiendas y asigna el `store_segment`.
- El **nivel de servicio se modula por el segmento**: el de **alto volumen** recibe z=1.65 (~95 %) y
  el resto z=1.28 (~90 %). El segmento grande se deduce **leyendo `centroides_unidades` del meta**
  (máximo `venta_media`), no por una constante (`_segmento_alto_volumen`, `almacen_service.py:43`).

### 2.3. Proxy de demanda + dimensionado del stock

- `_demanda_reciente` estima media y desviación de la demanda diaria de los últimos 28 días.
- `stock_seguridad = z · σ · √lead` (o un fallback del 50 % de la demanda en lead si la serie es muy
  corta); `recommended_stock = demanda_lead + stock_seguridad`; `stockout_risk = stock_actual <
  recommended_stock`.

### 2.4. Esquemas y errores

`schemas/almacen.py` con `extra="forbid"`, `high_demand_probability ∈ [0,1]`, `lead_time_days`
opcional. Producto sin histórico → `SolicitudInvalida` (**400**). `metadata` reporta la definición de
demanda alta y el umbral de probabilidad **leído del meta**.

## 3. Código puntual

| Qué | Dónde |
|-----|-------|
| Endpoint `POST /inventory` | `src/spc/api/routers/almacen.py:18` |
| Ensamblado clasificación + clustering + proxy | `src/spc/service/almacen_service.py:109` (`alertas`) |
| Clase/proba de la última observación por serie | `almacen_service.py:61` (`_clases_por_serie`) |
| Segmento de alto volumen leído del meta | `almacen_service.py:43` (`_segmento_alto_volumen`) |
| Nivel de servicio modulado por segmento | `almacen_service.py:151-158` |
| Esquema request/response | `src/spc/api/schemas/almacen.py` |

## 4. Resultados (contrato de la respuesta)

```json
{
  "field": "inventory",
  "alerts": [
    {
      "store_id": "1",
      "product_id": "BEVERAGES",
      "demand_class": "high",
      "high_demand_probability": 0.87,
      "stockout_risk": true,
      "recommended_stock": 1600.0,
      "safety_stock": 420.0,
      "store_segment": 1
    }
  ],
  "metadata": {
    "threshold": "high_demand = sales > P75 of its family",
    "probability_threshold": 0.3185
  }
}
```

- `probability_threshold` = umbral real del artefacto (HU-003), no una constante en el código.
- `store_segment` viene del clustering de tiendas (HU-004) y afina el `safety_stock`.
- Cobertura de tests (`tests/api/test_almacen_api.py`): válido + inválidos (producto sin histórico →
  400, rango inválido → 422), **nunca un 500**.

## 5. Cómo probar

```powershell
venv\Scripts\python -m uvicorn spc.api.main:app
Invoke-RestMethod -Uri http://127.0.0.1:8000/inventory -Method Post `
  -ContentType "application/json" -InFile examples\api\almacen_request.json |
  ConvertTo-Json -Depth 6
venv\Scripts\python -m pytest tests/api/test_almacen_api.py -q
```

## 6. 📸 Capturas sugeridas

1. **Respuesta 200 de `/inventory`** — Swagger *Try it out* con `examples/api/almacen_request.json`
   → **Code 200** con la lista `alerts` (mostrando `demand_class`, `stockout_risk`,
   `recommended_stock`, `store_segment`).
2. **Código del ensamblado** — `src/spc/service/almacen_service.py`, función `alertas`
   (líneas 140–172, donde se calcula `stockout_risk` y se modula el nivel de servicio por segmento).
3. **Segmento leído del meta** — `almacen_service.py:43` (`_segmento_alto_volumen`) para evidenciar
   que el valor de negocio no está hard-codeado.
4. **Esquema de respuesta** — `src/spc/api/schemas/almacen.py`, clase `AlertaItem` (los campos del
   contrato).
5. **Tests en verde** — terminal de `pytest tests/api/test_almacen_api.py -q`.
</content>
