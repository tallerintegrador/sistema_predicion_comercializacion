# Instalacion COMPLETA para desarrollo local (runtime + legacy).
#
# La fuente de verdad es pyproject.toml. Para el servicio en produccion use el
# subconjunto lean `requirements-api.txt`. Este archivo suma las deps del extra
# `legacy` (EDA Favorita + entrenamiento retail offline archivado en legacy/).

# --- Runtime (== requirements-api.txt) ---
-r requirements-api.txt

# --- Legacy (extra `legacy` de pyproject): solo para revivir el pipeline archivado ---
matplotlib==3.10.9
seaborn==0.13.2
nbformat==5.10.4
optuna==4.1.0
jupyter==1.1.1
ipykernel==7.3.0
